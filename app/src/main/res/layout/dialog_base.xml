<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="260dp"
    android:layout_height="match_parent"
    android:background="@drawable/bg_dialog_bg"
    android:orientation="vertical"
    android:padding="15dp">

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:text="title"
        android:textColor="@color/black_363636"
        android:textSize="@dimen/text_size_title"
        android:visibility="gone" />

    <FrameLayout
        android:id="@+id/fl_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <RelativeLayout
        android:id="@+id/rl_opt_button_layout"
        android:layout_width="match_parent"
        android:layout_height="35dp"
        android:layout_marginTop="10dp"
        android:visibility="gone">

        <Button
            android:id="@+id/btn_negative"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_toLeftOf="@+id/btn_positive"
            android:background="@drawable/selector_btn_dialog_opt"
            android:minWidth="60dp"
            android:text="取消"
            android:textColor="@color/black_363636"
            android:textSize="@dimen/text_size_content"
            android:visibility="gone" />

        <Button
            android:id="@+id/btn_positive"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_alignParentRight="true"
            android:background="@drawable/selector_btn_dialog_opt"
            android:minWidth="60dp"
            android:text="确定"
            android:textColor="@color/black_363636"
            android:textSize="@dimen/text_size_content"
            android:visibility="gone" />

        <com.jake.health.ui.widgt.MaterialProgressBar
            android:id="@+id/mpb_progress_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true" />
    </RelativeLayout>

</LinearLayout>